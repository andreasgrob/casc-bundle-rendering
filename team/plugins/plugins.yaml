# This file is automatically generated - please do not edit manually.

# Annotations (given as a comment above the plugin in question):
#  tag:custom:version=...    - set a custom version (e.g. 1.0)
#  tag:custom:url=...        - sets a custom url (e.g. https://artifacts.acme.test/my-plugin/1.0/my-plugin.jpi)
#  tag:custom:requires=...   - spaced separated list of required dependencies (e.g. badge envinject)

# Plugin Categories:
#  cap - is this a CAP plugin?
#  3rd - is this a 3rd party plugin?
#  old - is this a deprecated plugin?
#  cve - are there open security issues?
#  bst - installed by default
#  dep - installed as dependency
#  lst - installed because it was listed
#  src - used as a source plugin for this list

plugins:
  - id: ant # cap dep
  - id: artifactory # 3rd lst
  - id: cloudbees-casc-client # cap lst
  - id: cloudbees-casc-items-controller # cap lst
  - id: cloudbees-github-reporting # cap lst
  - id: cloudbees-groovy-view # cap lst
  - id: cloudbees-hashicorp-vault # cap lst
  - id: cloudbees-inactive-items # cap lst
  - id: cloudbees-jenkins-advisor # cap lst
  - id: cloudbees-monitoring # cap lst
  - id: cloudbees-nodes-plus # cap lst
  - id: cloudbees-pipeline-explorer # cap lst
  - id: cloudbees-quiet-start # cap lst
  - id: cloudbees-ssh-slaves # cap lst
  - id: cloudbees-view-creation-filter # cap lst
  - id: cloudbees-workflow-template # cap lst
  - id: cloudbees-workflow-ui # cap lst
  - id: config-file-provider # cap dep
  - id: configuration-as-code # cap lst dep
  - id: email-ext # cap lst
  - id: git-server # cap lst
  - id: github-branch-source # cap lst
  - id: gradle # cap dep
  - id: infradna-backup # cap lst
  - id: javadoc # cap dep
  - id: jsch # cap dep
  - id: junit # cap dep
  - id: matrix-project # cap dep
  - id: maven-plugin # 3rd dep
  - id: operations-center-cloud # cap lst
  - id: saml # cap lst
  - id: workflow-aggregator # cap lst
